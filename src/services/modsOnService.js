
import modsOn from '../data/mods_on.json';

const fetchModsOn = async () => {
    // Placeholder for future API call
  
    // For now, we're just using the local JSON file
    return Promise.resolve(modsOn);
};

export default {
    fetchModsOn
};
