
import usrsOn from '../data/users_on.json';

const fetchUsrsOn = async () => {
    // Placeholder for future API call
  
    // For now, we're just using the local JSON file
    return Promise.resolve(usrsOn);
};

export default {
    fetchUsrsOn
};
